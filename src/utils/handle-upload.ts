// import { uploadImage } from "@/utils/cloudinary";

// export default async function handleUpload(file: File) {
//   const result = await (await uploadImage(file)).json();

//   return result.url;
// }
